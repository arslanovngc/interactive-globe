export const MAP_WIDTH = 900;
export const MAP_HEIGHT = 640;
export const CARD_ANCHOR = { x: 890, y: 330 };

export const TEXTURE_WIDTH = 8192;
export const TEXTURE_HEIGHT = 4096;

export const CAMERA_DISTANCE = 10.5;
export const MIN_CAMERA_DISTANCE = 8.2;
export const MAX_CAMERA_DISTANCE = 13.5;
export const GLOBE_RADIUS = 2.65;

export const round = (value: number) => Math.round(value * 1000) / 1000;
